<% (['development', 'production'] + [@rails_env]).uniq.each do |env| -%>
<%= env %>:
  <% @environment_variables.each do |key,val| -%>
  <%= key.to_s.inspect %>: <%= val.to_s.inspect %>
  <% end -%>
<% end %>